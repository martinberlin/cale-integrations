{% extends '/base.html.twig' %}
{% set carousel_high = 110 %}

{% block stylesheets %}
    <style>
        #myCarousel.carousel {
            height: {{ carousel_high }}px;
        }
        #myCarousel .item {
            height: {{ carousel_high }}px; width: 100%;
            background-color: #333;
        }
    </style>
{% endblock stylesheets %}

{% block carousel %}
    {% include "/partials/carousel.html.twig" %}
{% endblock carousel %}

{% block body %}
    <h3>API Integrations supported</h3>
    <p>This is a list generated from our database with the APIs that are currently integrated and ready to use in our system as soon
    as they are configured with your authorization keys.</p>
    <br>
    <table style="border-spacing:4px;width: 99%">
        <thead>
        </tr>
        <th>API Name</th>
        <th>Category</th>
        <th title="Documentation">Docs</th>
        <th>Authentication</th>
        </tr>
        </thead>

        <tbody>
        {% for a in apis %}
            <tr style="border:1px #1b6d85 dashed;height: 4em">
                <td>&nbsp;<b>{{ a.name }}</b> </td>
                <td><small>{{ a.category.name }}</small></td>
                <td><small>
                    <a href="{{ a.documentationUrl }}" class="alert alert-info"><span class="glyphicon glyphicon-link"></span></a></small>
                </td>
                <td>
                    {% if a.id == 6 %}
                        <a href="{{ path('www_api-cloudwatch') }}">{{ 'nav_api_cloudwatch'|trans }}</a>
                    {% else %}
                        {{ a.authNote }}
                    {% endif %}
                </td>
            </tr>
        {% endfor %}
        <tr style="border:1px #1b6d85 dashed;height: 4em">
            <td>&nbsp;<b>Github issues</b> </td>
            <td><small>Development & IT</small></td>
            <td><small>
                <a href="https://developer.github.com/v3/" target="_blank" class="alert alert-info">
                    <span class="glyphicon glyphicon-link"></span></a></small>
            </td>
            <td><small>Planned May'20</small></td>
        </tr>
        <tr style="border:1px #1b6d85 dashed;height: 4em">
            <td>&nbsp;<b>Climacell.co</b> </td>
            <td><small>Weather</small></td>
            <td><small>
                <a href="https://www.climacell.co/weather-api/docs/" target="_blank" class="alert alert-info">
                    <span class="glyphicon glyphicon-link"></span></a></small>
            </td>
            <td><small>In development</small></td>
        </tr>
        </tbody>
    </table>
    <br>
    UPDATE: We recently added an <a href="{{ path('www_api-ical') }}">Internet Calendaring support API</a> enabling our users to get in sync with any Calendar provider.
    <p>If the application programming interface you want to use is not listed here, please send us a message on twitter mentioning @martinfasani or after logging into our Backend simply use
        <b>User <span class="glyphicon glyphicon-arrow-right"></span> Get support</b>
    </p>
{% endblock %}

{% block javascripts %}
    <script type="text/javascript" src="/js/engine/common.js"></script>
{% endblock %}