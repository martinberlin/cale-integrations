{% extends '/base.html.twig' %}
{% set carousel_high = 110 %}

{% block stylesheets %}
    <style>
        #myCarousel.carousel {
            height: {{ carousel_high }}px;
        }
        #myCarousel .item {
            height: {{ carousel_high }}px; width: 100%;
            background-color: #333;
        }
    </style>
{% endblock stylesheets %}

{% block carousel %}
    {% include "/partials/carousel.html.twig" %}
{% endblock carousel %}

{% block body %}
    <h3>API-Integrationen unterstützt</h3>
    <p>ies ist eine Liste, die aus unserer Datenbank mit den APIs generiert wird, die derzeit in unser System integriert
        und einsatzbereit sind, sobald sie mit Ihren Autorisierungsschlüsseln konfiguriert wurden.</p>
    <br>
    <table style="border-spacing:4px;width: 99%">
        <thead>
        </tr>
        <th>API</th>
        <th>Kategorie</th>
        <th title="Documentation">Docs</th>
        <th>Authentication</th>
        </tr>
        </thead>

        <tbody>
        {% for a in apis %}
            <tr style="border:1px #1b6d85 dashed;height: 4em">
                <td>&nbsp;<b>{{ a.name }}</b> </td>
                <td><small>{{ a.category.name }}</small></td>
                <td><small>
                    <a href="{{ a.documentationUrl }}" class="alert alert-info"><span class="glyphicon glyphicon-link"></span></a></small>
                </td>
                <td>
                    {% if a.id == 6 %}
                        <a href="{{ path('www_api-cloudwatch') }}">{{ 'nav_api_cloudwatch'|trans }}</a>
                    {% else %}
                        {{ a.authNote }}
                    {% endif %}
                </td>
            </tr>
        {% endfor %}
        <tr style="border:1px #1b6d85 dashed;height: 4em">
            <td>&nbsp;<b>Github issues</b> </td>
            <td><small>Development & IT</small></td>
            <td><small>
                <a href="https://developer.github.com/v3/" target="_blank" class="alert alert-info">
                    <span class="glyphicon glyphicon-link"></span></a></small>
            </td>
            <td><small>Planned May'20</small></td>
        </tr>

        </tbody>
    </table>
    <br>
    <p>Wenn die Anwendungsprogrammierschnittstelle, die Sie verwenden möchten, hier nicht aufgeführt ist, senden Sie uns
        auf Twitter eine Nachricht mit dem Hinweis auf @martinfasani oder verwenden Sie sie einfach, nachdem Sie sich in
        unser Backend eingeloggt haben
        <b>User <span class="glyphicon glyphicon-arrow-right"></span> Get support</b>
    </p>
{% endblock %}

{% block javascripts %}
    <script type="text/javascript" src="/js/engine/common.js"></script>
{% endblock %}